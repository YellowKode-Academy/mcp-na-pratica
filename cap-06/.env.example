# Variaveis de ambiente -- cap-06 (com JWT)
# Copie para .env e preencha os valores reais

SERPAPI_KEY=
MAX_RESULTADOS=10

# Gere JWT_SECRET com: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24
